# TOPECH 网站部署指南 — topechltd.com（GitHub Pages 方案）

> 站点特点：纯静态、100% 相对路径引用，部署时无需修改任何代码。
> 部署目标：网站文件放在**域名根目录**（不要再套 /topech/ 子目录），与 SEO canonical 设置保持一致。

---

## 第一步：创建 GitHub 仓库并上传文件

1. 在 GitHub 新建仓库（如 `topechltd-web`，Public 或 Private 均可，Pages 免费版需 Public）
2. 上传**整个项目文件夹内容**（注意是文件夹里面的内容，不是文件夹本身）：

```
必须上传的目录和文件：
├── index.html          ← 首页
├── products.html       ← 产品列表页
├── applications.html   ← 应用场景页
├── resources.html      ← 技术资源页
├── contact.html        ← 联系页
├── sitemap.xml         ← Google 收录用
├── robots.txt          ← 搜索引擎/AI 爬虫规则
├── CNAME               ← GitHub Pages 自定义域名（已生成，内容 topechltd.com）
├── css/                ← 样式表（漏传会全站无样式）
├── js/                 ← 交互脚本
├── images/             ← 所有产品图片（漏传会出现破图）
├── videos/             ← 测试视频
└── products/           ← 19 个产品详情页
```

**不需要上传**：`*.ps1` 脚本文件（build-products.ps1 等仅为本地生成工具）。
**建议清理**：`images/hero/` 下的 `_dbg*.jpg`、`_debug*.jpg` 调试残留图可删除后再传。

---

## 第二步：开启 GitHub Pages

1. 仓库 → Settings → Pages
2. Source 选 **Deploy from a branch**，Branch 选 `main`，文件夹选 `/ (root)`，保存
3. Custom domain 栏填入 `topechltd.com`（因为仓库里已有 CNAME 文件，这里会自动识别）
4. 勾选 **Enforce HTTPS**（证书自动签发，首次需等几分钟）

---

## 第三步：域名 DNS 解析（在 topechltd.com 的域名服务商处操作）

主域名需要 **4 条 A 记录**指向 GitHub 服务器：

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| @ | A | 185.199.108.153 |
| @ | A | 185.199.109.153 |
| @ | A | 185.199.110.153 |
| @ | A | 185.199.111.153 |

可选（推荐，让 www.topechltd.com 也能访问）：

| 主机记录 | 记录类型 | 记录值 |
|---|---|---|
| www | CNAME | 你的用户名.github.io |

DNS 生效一般需 10 分钟～24 小时，之后访问 https://topechltd.com 即可看到网站。

---

## 第四步：验证部署（必做）

部署完成后逐项检查：

- [ ] 首页样式正常（证明 css/ 已上传）
- [ ] 首页主图、精选产品 4 张卡片图显示（证明 images/ 已上传）
- [ ] 打开任意产品详情页，图片画廊和 FAQ 折叠区正常
- [ ] 抗磨试验机页面两个视频可播放（证明 videos/ 已上传）
- [ ] 浏览器按 F12 → Network 面板无 404 红色项
- [ ] 访问 https://topechltd.com/sitemap.xml 能看到 24 条 URL
- [ ] 访问 https://topechltd.com/robots.txt 能看到爬虫规则

---

## 第五步：提交 Google 收录（SEO 生效的关键）

1. 打开 [Google Search Console](https://search.google.com/search-console)
2. 添加资源 → 选"网域"类型，输入 `topechltd.com`，按提示做 DNS TXT 验证
3. 验证通过后 → 左侧"站点地图" → 输入 `sitemap.xml` → 提交
4. 等待 1～3 天，用"网址检查"工具可主动请求收录各页面

可选加分项：
- [Google Merchant Center](https://merchants.google.com) 提交产品数据（产品页已有 Product 结构化数据，利于 Google 购物类展示）

---

## 后续更新网站的流程

本地修改后（例如改了产品数据重跑 build-products.ps1），把**变动的文件**重新上传到仓库即可，GitHub Pages 约 1 分钟内自动更新。常见变动对照：

| 本地改动 | 需上传的文件 |
|---|---|
| 改产品数据（build-products.ps1 后重新生成） | products/ 下对应的 html |
| 换产品图片 | images/products/ 对应图片 + 相关 html |
| 改首页/列表页 | index.html / products.html |
| 新增页面 | 新 html + sitemap.xml 加一行 |

⚠️ 经验教训：之前线上破图全部源于**只传了 HTML 没传图片文件**，每次换图请 HTML 和图片一起上传。
